/*
Lista avaliativa sobre registros (entrega até 15/09)
https://ava.ufms.br/mod/quiz/view.php?id=320938
*/

Das seguintes afirmações, marque as opções verdadeiras.

a. Os registros (structs) podem conter apenas um tipo de dado.
Verdadeira

b. Os registros não podem ser comparados diretamente.


c. Os campos de diferentes registros (structs) devem ter nomes exclusivos.


d. Os registros (structs) são sempre passadas a funções por meio de chamadas por referência.


e. Os registros podem ser atribuídos diretamente.


f. O nome da tag (tipo) de um registro (structs) é opcional.